APP_NAME=Options Movement Analysis
DEBUG=false
HOST=0.0.0.0
PORT=8000

# CORS — frontend origin (Vite dev server default)
FRONTEND_ORIGIN=http://localhost:5173

# File paths (relative to backend working directory)
CONFIG_FILE=upstox_config.txt
RESULTS_DIR=results
